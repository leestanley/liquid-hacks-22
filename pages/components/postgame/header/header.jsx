import React from 'react';

import styles from '../../../../styles/components/postgame/header/Header.module.scss';

const Header = (props) => {
  return (
    <div className={styles.container}>
    </div>
  );
}

export default Header;